apiVersion: v1
kind: Pod
metadata:
  name: devpod-placeholder
spec:
  containers:
    - name: devpod
      resources:
        limits:
          nvidia.com/gpu: "1"
        requests:
          nvidia.com/gpu: "1"
          memory: "32Gi"
          cpu: "8"
      volumeMounts:
        - name: dshm
          mountPath: /dev/shm
        - name: new-pvc
          mountPath: /mnt/new-pvc
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 32Gi
    - name: new-pvc
      persistentVolumeClaim:
        claimName: new-pvc
  tolerations:
    - key: "nvidia.com/gpu"
      operator: "Exists"
      effect: "NoSchedule"
